let initialState = {
    baseURL: "https://amg-api.herokuapp.com/"
}

function reducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default reducer;