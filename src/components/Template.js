import React from 'react'
import {
    View, 
    Text,
    TouchableHighlight,
    StyleSheet
} from 'react-native'

export default function Name(navigator){
    return(
        <View style={styles.container} >

        </View>
    )
}

let styles = StyleSheet.create({
    container:{
        flex:1
    }
})